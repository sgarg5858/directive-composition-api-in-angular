import { CanColorDirective } from './can-color.directive';

describe('CanColorDirective', () => {
  it('should create an instance', () => {
    const directive = new CanColorDirective();
    expect(directive).toBeTruthy();
  });
});
