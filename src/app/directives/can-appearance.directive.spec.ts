import { CanAppearanceDirective } from './can-appearance.directive';

describe('CanAppearanceDirective', () => {
  it('should create an instance', () => {
    const directive = new CanAppearanceDirective();
    expect(directive).toBeTruthy();
  });
});
