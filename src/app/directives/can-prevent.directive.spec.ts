import { CanPreventDirective } from './can-prevent.directive';

describe('CanPreventDirective', () => {
  it('should create an instance', () => {
    const directive = new CanPreventDirective();
    expect(directive).toBeTruthy();
  });
});
